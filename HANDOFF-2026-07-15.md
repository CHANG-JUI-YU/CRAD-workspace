# Card Workspace 工作交接

## 目的與環境

後續 `C:\AI\projects\card-workspace` 修改移至新對話，避免目前 Session 與測試中的 OpenCode Agent 共用工作區而互相干擾。

- Windows / PowerShell 5.1，`pnpm@10.34.5`，Node `>=20.17 <21`
- 目前不是 Git repository，不可依賴 `git diff/status`。
- 先讀 `C:\AI\projects\card-workspace\CONTEXT.md`。

## 已完成工作

- Director `steps` 已在 `opencode.jsonc` 從 6 提高至 30；需重啟 OpenCode。
- Director 必須使用 `question` 等待使用者，不得自問自答或跨 Gate。
- `workflow_start` 強制 Intake 完成：至少一筆 answer、`no-additional-settings` complete，以及 `confirmed_no_additional_settings: true`；否則 `WORKFLOW_INTAKE_INCOMPLETE`。
- Blueprint 已移除角色與 greetings Token 預算，保留世界設定及全域預算。
- 非 `original` 入口仍 fail closed：`WORKFLOW_ENTRY_NOT_IMPLEMENTED`。
- Zhuji Creator 已按 `output_kind` 分流，使用 Task context、Lease、revision、CAS、`proposal@1` 與 `character_submit_proposal`。
- 七個 references 位於 `.agents/skills/zhuji-creation/references/module-*.md`，已綁入 registry 與 OpenCode prompt。
- Creator Skill、generation guide、example 與 fixture 已改用 structured `module.data`，不再產生 Markdown `content/sections`。

## Structured Zhuji 設計

- `packages/schemas/src/zhuji.ts` 定義七個 module-specific structured schemas。
- 新格式保留 `schema_version`、`mode`、`module`、`title`、`compile`、`provenance`、`extensions`、`data`。
- `zhujiModuleSchema` 是 structured/legacy union，既有 `content/sections` 文件仍可載入。
- `packages/schemas/src/proposal.ts` 的直接 `kind: zhuji` Proposal 強制使用 `structuredZhujiModuleSchema`，legacy Creator Proposal 會被拒絕。
- 雙向模式轉換仍保留 union 相容性。
- `packages/compiler/src/normalize.ts` 將 `data` canonical YAML 序列化為單一 fragment。
- `packages/ingestion/src/provenance.ts` 對 structured module 只建立 main provenance fragment。
- 不自動遷移既有 legacy 文件；Proposal 套用後才替換該模組。

## 七模組固定頂層鍵

- `appearance`：`外顯核心`、`面貌`、`身體基礎數據`、`性器官特徵`、`其他器官特徵`、`聲音`、`服裝風格與著裝習慣`、`交互模式`、`整體感官體驗`
- `inner_nature`：`人物核心定義`、`基礎組成`、`內在驅動力`、`情感架構`、`潛意識與精神衝突`、`行為偏好與習慣`、`過去經歷`、`極端情緒`、`人際網絡與歸屬感`、`內質的生動表達`
- `extension`：`人物核心定義`、`背景設定與成長經歷`、`人際關係`、`社會階層與經濟狀況`、`行為模式`、`情緒表現特徵`、`生活習慣與個性特徵`、`外延的生動表達`
- `expanded_extension`：`對 {{user}}`、`性相關`、`人物意象`、`私人空間營造`、`差異表現補充`
- `trait_refinement`：`性格特質` array
- `scene_dialogue`：`核心標籤與特質的風格表現`、`對 {{user}}`、`場景刻畫` array
- `self_introduction`：`核心標籤與特質的風格表現`、`對 {{user}}`、`外評觀價`、`性格基礎`

## 關鍵檔案

- `packages/schemas/src/zhuji.ts`
- `packages/schemas/src/proposal.ts`
- `packages/compiler/src/normalize.ts`
- `packages/ingestion/src/provenance.ts`
- `packages/schemas/test/author-schemas.test.ts`
- `packages/schemas/test/workflow-contracts.test.ts`
- `packages/compiler/test/normalize-planner.test.ts`
- `packages/ingestion/test/provenance.test.ts`
- `packages/workflow/test/proposal-validation.test.ts`
- `packages/workflow/test/proposal-apply.test.ts`
- `packages/mcp-server/test/author-tools.test.ts`
- `.agents/skills/zhuji-creation/`
- `.opencode/prompts/zhuji-creator.md`
- `workflow/agent-registry.yaml`
- `opencode.jsonc`

## 驗證結果

- `npx --yes pnpm@10.34.5 check`：通過。
- 59 files、353 tests：全部通過。
- `npx --yes pnpm@10.34.5 test:coverage`：通過。
- Coverage：statements/lines 88.10%、branches 80.19%、functions 88.23%。
- 門檻不可降低：statements/lines/functions 85%、branches 80%。
- `npx --yes pnpm@10.34.5 agent-lint`：`{"ok":true,"diagnostics":[]}`。

## 下一步

1. 重啟 OpenCode，讓 Director steps 與 references binding 生效。
2. 使用新 project ID 做真模型七模組 Smoke Test，不要污染現有專案。
3. 確認實際 Proposal 使用 `module.data` 並通過 `proposal@1`。
4. 確認套用後 YAML 保留深層 object/array，不是 Markdown 字串。
5. 編譯 Preview，確認 structured YAML 進入世界書 fragment。
6. 由使用者執行 SillyTavern 匯入與人工驗收。
7. 若子結構仍太扁平，先加強 module references 的子鍵模板；不可移除 legacy union 或降低 coverage。

## 現有專案與外部參考

- `projects/kitou-ran` 舊狀態為 Blueprint stage、revision 2，曾有過期 lease；操作前重新查詢，勿沿用舊 revision。
- `projects/kitou-ran/characters/character-1/zhuji/02-inner-nature.yaml` 是 legacy 範例，未自動遷移是預期行為。除非使用者指定，不要直接覆寫。
- 七模組舊 references：`C:\Users\user\Downloads\新增資料夾\card-workspace-backup\.agents\skills\st-creator-zhuji-skill\references`
- 原版內質：`C:\Users\user\Downloads\新增資料夾\新增資料夾\雪之下陽乃\模組2_內質.yaml`
- 外部路徑再次讀取前先取得批准，且只做唯讀存取。

## 工程限制

- 人工修改使用 `apply_patch`。
- 不得還原其他 Agent 或使用者的變更。
- 真模型 Smoke Test 與 SillyTavern 驗收由使用者主導。
- structured schema 目前只硬性固定頂層鍵，內部允許 JSON object/array。若要逐子鍵硬性 schema，先完整比對七份舊 reference，不可猜測。

## Suggested Skills

- `director-orchestration`：Director 初始化、訪談、委派、Gate、Preview。
- `zhuji-creation`：Zhuji Creator 產生 proposal。
- `character-critique`：唯讀檢查角色產物。
- `diagnose`：Smoke Test、Proposal 或編譯失敗。
- 新功能或行為變更前使用 `brainstorming`。

## 尚未完成

- 重啟後的真模型七模組端到端 Smoke Test。
- SillyTavern 匯入與人工角色品質驗收。
