{
  "name": "@st-workspace/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@st-workspace/adapters": "workspace:*",
    "@st-workspace/core": "workspace:*",
    "@st-workspace/compiler": "workspace:*",
    "@st-workspace/runtime": "workspace:*",
    "@st-workspace/server": "workspace:*"
  }
}
