---
name: html-critique
description: 只在 HTML Creator Critic 唯讀檢查 typed HTML plugin proposal 時使用。
---

# HTML Critique

檢查 html-policy allowlist、element/attribute safety、root selector scope、CSS/HTML reparse、paired regex、greeting IDs、MVU writable path與CAS validation。所有 finding 必須有 exact evidence與target revision。

只提交 `review-report@1`，不得修改 proposal、批准 review 或簽發 user token。
